// Shell
Use Cmder as the shell for all terminal commands.

// Cmder Initialization
Initialize Cmder session only once per session:
cmd.exe /k "D:\Cmder\vendor\init.bat"

// Terminal Commands
All terminal commands should be run in Cmder shell.
Use regular commands without Cmder initialization after it's already running.

// Environment
- Cmder should be installed at D:\Cmder
- Node.js and npm should be available in Cmder environment
- Git should be available in Cmder environment

// Validation
To verify Cmder is working correctly, run:
node -v
npm -v

If Cmder is not working properly:
1. Initialize Cmder again
2. Verify it works with node -v and npm -v
3. Continue using regular commands
